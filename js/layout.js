var layout_button = document.querySelector("#layout_button");

layout_button.addEventListener("click", perform_layout);

function perform_layout () {
	spring_layout();
	// Fruch_layout();
	// tut_layout();
}
